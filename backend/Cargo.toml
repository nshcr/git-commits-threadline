[package]
name = "git-commits-threadline"
version = "0.1.0"
edition = "2021"

[dependencies]
gix = { version = "0.80", default-features = false, features = [
    "max-performance-safe",
    "revision",
] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
